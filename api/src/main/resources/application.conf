ktor {
    deployment {
        port = 8004
        port = ${?API_PORT}
    }
}

# No database configuration needed for MVP - using in-memory RAG
# Future: Add PostgreSQL if necessary

ollama {
    baseUrl = "http://localhost:11434"
    baseUrl = ${?OLLAMA_BASE_URL}
    model = "llama3.2:3b"
    model = ${?OLLAMA_MODEL}
}